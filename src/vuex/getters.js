export const currId = state => state.currId;

export const msgs = state => state.msgs;

export const users = state => state.users;

export const uname = state => state.uname;

export const uid = state => state.uid;

export const danmaku = state => state.danmaku;

export const moduleId = state => state.moduleId;

export const imgs = state => state.imgs;